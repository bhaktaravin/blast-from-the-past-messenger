# 🚂 Railway Deployment Guide

## Why Switch to Railway?

✅ **Simpler** than Fly.io
✅ **Free tier** with $5/month credit
✅ **Auto-deploys** from GitHub
✅ **Built-in PostgreSQL** addon
✅ **Better dashboard** and logs
✅ **No credit card** required for free tier

---

## Quick Start (5 minutes)

### 1. Sign Up for Railway

1. Go to https://railway.app
2. Click "Start a New Project"
3. Sign in with GitHub

### 2. Deploy from GitHub

1. Click "New Project"
2. Select "Deploy from GitHub repo"
3. Choose: `blast-from-the-past-messenger`
4. Railway auto-detects it's a Rust app!

### 3. Add PostgreSQL

1. In your project, click "New"
2. Select "Database" → "PostgreSQL"
3. Railway automatically creates `DATABASE_URL` variable

### 4. Configure Environment Variables

Add these in Railway dashboard → Variables:

```bash
BIND_ADDR=0.0.0.0:$PORT
DATABASE_URL=(auto-generated by Railway)
```

**Note:** Railway automatically provides `$PORT` variable

### 5. Deploy!

- Push to GitHub → Railway auto-deploys
- Or click "Deploy" in Railway dashboard

---

## Configuration Files

Railway uses these files (already created):

### `nixpacks.toml`
```toml
[phases.setup]
nixPkgs = ['nodejs_20', 'rustc', 'cargo', 'gcc', 'postgresql']

[phases.build]
cmds = ['cargo build --release --bin server --features server']

[start]
cmd = './target/release/server'
```

### `railway.toml` (optional)
```toml
[build]
builder = "nixpacks"

[deploy]
startCommand = "./target/release/server"
restartPolicyType = "on_failure"
```

---

## Environment Variables

| Variable | Value | Required |
|----------|-------|----------|
| `DATABASE_URL` | Auto-generated | ✅ Yes |
| `BIND_ADDR` | `0.0.0.0:$PORT` | ✅ Yes |
| `PORT` | Auto-set by Railway | ✅ Auto |

---

## Migration from Fly.io

### Before (Fly.io):
```bash
fly launch
fly deploy
fly postgres create
fly secrets set DATABASE_URL=...
```

### After (Railway):
```bash
# Just connect GitHub - that's it! 🎉
# Railway handles everything automatically
```

---

## Comparison

| Feature | Fly.io | Railway |
|---------|--------|---------|
| **Setup Time** | 10-15 min | 2-5 min |
| **Config Files** | fly.toml + Dockerfile | Optional |
| **Database** | Separate setup | 1-click addon |
| **Auto-deploy** | Manual setup | Built-in |
| **Free Tier** | $5 credit | $5/month credit |
| **Dashboard** | CLI-focused | Web GUI |
| **Logs** | `fly logs` | Built-in viewer |
| **Learning Curve** | Steep | Gentle |

---

## Step-by-Step Railway Setup

### 1. Create Railway Account
```bash
# No CLI needed! Everything is web-based
```

Go to: https://railway.app/new

### 2. Connect GitHub
- Authorize Railway to access your repos
- Select: `blast-from-the-past-messenger`

### 3. Add PostgreSQL
```
Project → New → Database → PostgreSQL
```

Railway automatically:
- Creates database
- Sets `DATABASE_URL` 
- Connects to your app

### 4. Configure Server
In Railway dashboard → Settings:

**Build Command:**
```bash
cargo build --release --bin server --features server
```

**Start Command:**
```bash
./target/release/server
```

### 5. Set Environment Variables
```bash
BIND_ADDR=0.0.0.0:$PORT
# DATABASE_URL is auto-set by PostgreSQL addon
```

### 6. Deploy
- Click "Deploy"
- Or push to GitHub (auto-deploys)

---

## Getting Your URL

After deployment:
1. Go to Settings → Domains
2. Railway provides: `your-app.up.railway.app`
3. Optional: Add custom domain

Update your client to use:
```
wss://your-app.up.railway.app
```

---

## Database Migration

### Export from Fly.io:
```bash
fly postgres connect -a your-db-name
pg_dump > backup.sql
```

### Import to Railway:
```bash
# Get Railway connection string from dashboard
psql $DATABASE_URL < backup.sql
```

Or use Railway's built-in migration tools!

---

## Monitoring

### Logs
```
Railway Dashboard → Deployments → View Logs
```

### Metrics
```
Railway Dashboard → Metrics
- CPU usage
- Memory usage
- Network traffic
```

### Restart
```
Railway Dashboard → Deployments → Restart
```

---

## Pricing

### Free Tier
- $5 credit per month
- Enough for hobby projects
- No credit card required

### Pro Plan ($20/month)
- $20 credit per month
- Priority support
- Advanced features

### Your Server Estimate:
- **CPU:** ~$0.01/hour (~$7/month)
- **Memory:** Included
- **Database:** ~$5/month
- **Total:** ~$12/month

**Free tier covers most usage!** ✅

---

## Troubleshooting

### Build Fails?

**Check:**
1. Is `server` feature enabled in Cargo.toml?
2. Are all dependencies compatible?
3. Check build logs in Railway dashboard

**Fix:**
```toml
# Cargo.toml should have:
[features]
server = []

[[bin]]
name = "server"
required-features = ["server"]
```

### Can't Connect to Database?

**Check:**
1. PostgreSQL addon is running
2. `DATABASE_URL` is set
3. Server is using Railway's `DATABASE_URL`

### Server Won't Start?

**Check:**
1. `BIND_ADDR` uses `$PORT` variable
2. Server listens on `0.0.0.0`, not `localhost`
3. Binary path is correct: `./target/release/server`

---

## Advanced: Custom Domain

1. Railway Dashboard → Settings → Domains
2. Click "Add Domain"
3. Enter: `messenger.yourdomain.com`
4. Add CNAME record to your DNS:
   ```
   messenger  CNAME  your-app.up.railway.app
   ```
5. Wait for SSL certificate (automatic)

---

## CI/CD with Railway

Railway automatically:
- ✅ Deploys on every push to `main`
- ✅ Shows build status
- ✅ Rolls back on failure
- ✅ Keeps deployment history

**No extra setup needed!**

---

## Comparison with Others

| Platform | Setup | Cost | Ease |
|----------|-------|------|------|
| Railway | ⭐⭐⭐⭐⭐ | $5-20 | 🟢 Easiest |
| Fly.io | ⭐⭐⭐ | $5-20 | 🟡 Medium |
| Heroku | ⭐⭐⭐⭐ | $7-25 | 🟢 Easy |
| AWS | ⭐ | Variable | 🔴 Hard |
| DigitalOcean | ⭐⭐ | $5-10 | 🟡 Medium |

**Railway is the easiest!** 🎉

---

## Support

- **Docs:** https://docs.railway.app
- **Discord:** https://discord.gg/railway
- **Status:** https://status.railway.app

---

## Quick Commands

```bash
# No CLI needed! But if you want:
npm install -g @railway/cli
railway login
railway up
railway logs
```

---

## Checklist

- [ ] Create Railway account
- [ ] Connect GitHub repo
- [ ] Add PostgreSQL addon
- [ ] Set environment variables
- [ ] Deploy
- [ ] Test WebSocket connection
- [ ] Update client with new URL
- [ ] Migrate database (if needed)
- [ ] Monitor logs
- [ ] Celebrate! 🎉

---

## Ready?

Just go to https://railway.app and click "New Project"!

It's **that** simple. 🚂✨
